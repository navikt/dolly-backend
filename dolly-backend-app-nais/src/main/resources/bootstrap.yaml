spring.cloud.vault:
  enabled: true
  host: vault.adeo.no
  port: 443
  connection-timeout: 15000
  read-timeout: 30000
  application-name: preprod/fss/dolly-backend

  authentication: TOKEN

  generic:
    enabled: false
  database:
    enabled: true
    role: brreg-stub-admin
    backend: postgresql/preprod-fss